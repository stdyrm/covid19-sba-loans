import React, { useState, useEffect } from 'react';
import "./modal.scss";

const Modal = ({ data, activeModal, setActiveModal }) => {
	useEffect(() => {
		console.log(activeModal)
	}, [activeModal]);

	return null;
};

export default Modal;
